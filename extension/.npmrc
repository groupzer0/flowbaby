# GitHub Packages registry for @groupzer0 scoped packages
@groupzer0:registry=https://npm.pkg.github.com

# Auth token read from environment variable (never hard-coded)
# Required scopes: read:packages
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
