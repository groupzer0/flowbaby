# Python Bridge Dependencies
# For TypeScript-Python subprocess communication with Cognee

# Core Cognee dependency
# Plan 059: Upgraded from 0.4.1 to 0.5.1 to enable filesystem session caching (CACHE_BACKEND=fs)
# Cognee 0.5.0+ introduces filesystem cache via diskcache (bundled), removing implicit Redis dependency
cognee>=0.5.1,<0.6.0

# Environment variable management
python-dotenv>=1.0.0

# Ontology parsing (TTL/RDF)
rdflib>=7.0.0

# Explicitly pin lancedb dependencies to prevent import errors
lancedb>=0.24.0
lance-namespace==0.0.21

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
